---
description: BYOK custom patch workflow — survive nightly upstream syncs
alwaysApply: true
---

# BYOK Custom Patches — Survive Nightly Syncs

This repo nightly-syncs from `microsoft/vscode-copilot-chat`. The sync workflow
(`.github/workflows/sync-upstream.yml`) resets tracked files to upstream HEAD
and then runs `.github/scripts/apply-byok-patches.sh`.

**Any custom code you write directly in source files will be wiped out the next
night unless you also add a patch to that script.**

## When to Add a Patch

Whenever you modify a file that also exists upstream and you want the change to
persist, you MUST add a corresponding patch to
`.github/scripts/apply-byok-patches.sh`.

Examples of code that MUST be patched:

- Modifications to `src/platform/**` (copilot auth, token management, model fetch)
- Modifications to `src/extension/**` that touch upstream files (agent intent,
  BYOK providers, prompt rendering, etc.)
- `package.json` metadata (name, description, version)
- Any file tracked by upstream

New files you add under a custom directory (e.g. `src/byok-custom/`) are safe —
the sync workflow doesn't touch untracked paths.

## Patch Design Rules

### 1. Idempotency

Every patch MUST be safe to run multiple times. Use a sentinel check:

```js
if (code.includes("BYOK CUSTOM PATCH: my-feature")) {
  console.log("my-feature already present, skipping");
  process.exit(0);
}
```

### 2. Sentinel Markers

Wrap inserted code with comment markers so future reviewers (and the sentinel
check) can find it:

```typescript
// ─── BYOK CUSTOM PATCH: my-feature ──────────────────────────
// Preserved by .github/scripts/apply-byok-patches.sh. Do not remove.
...your code...
// ─── END BYOK CUSTOM PATCH ──────────────────────────────────
```

### 3. Anchor Robustness

Prefer stable, rarely-changing strings as replacement anchors. Bad anchors are
long multi-line bodies that upstream might tweak. Good anchors:

- Import statements
- Function signatures
- Well-known exported constant names

If an anchor is missing, **log a warning and exit 0** — never fail the sync
workflow over a missing anchor:

```js
if (!code.includes(anchor)) {
  console.warn("WARN: anchor not found — skipping my-feature patch");
  process.exit(0);
}
```

### 4. Prefer Append Over Replace

When adding new exports/helpers, append at EOF rather than inserting in the
middle. EOF appends don't conflict with upstream edits.

When modifying behavior, insert a guard BEFORE the upstream logic rather than
replacing the upstream block entirely — this keeps the patch smaller and makes
upstream evolution less likely to break it.

### 5. Test Before Committing

Always test the patch in both modes:

```bash
# Fresh apply (simulates nightly sync on clean upstream)
git stash push -- <files-you-patched>
bash .github/scripts/apply-byok-patches.sh
git diff stash@'{0}' -- <files-you-patched>  # should match your manual edit
git stash drop

# Idempotent re-apply
bash .github/scripts/apply-byok-patches.sh   # should say "already present, skipping"
```

## Workflow Checklist

When making a custom change that touches upstream files:

- [ ] Make the change manually in the source file(s) with sentinel markers
- [ ] Write a corresponding patch in `.github/scripts/apply-byok-patches.sh`
- [ ] Use `if (code.includes(sentinel)) { skip }` for idempotency
- [ ] Gracefully skip on missing anchors (exit 0, log warning)
- [ ] Test fresh-apply: stash, run script, diff against stash, confirm match
- [ ] Test idempotency: run script twice, confirm second run skips
- [ ] Commit the source changes AND the script update together

## Existing Patches (as of this rule)

1. **Patch 1** — Fake token bypass in `copilotTokenManager.ts`
2. **Patch 2** — `getPrimaryType` for Gemini union types in `geminiFunctionDeclarationConverter.ts`
3. **Patch 3** — Rename extension + bump version + clamp `engines.vscode` to `^1.116.0` in `package.json`
4. **Patch 4** — Tiered auto-compaction helpers in `backgroundSummarizer.ts`
5. **Patch 5** — Fake-token bypass + 1-min backoff in `modelMetadataFetcher.ts`
6. **Patch 6** — Tier-3 synchronous compaction in `agentIntent.ts`
7. **Patch 7** — Readable Gemini errors in `geminiNativeProvider.ts` (strip JSON blob from `ApiError.message` before surfacing in chat UI)
8. **Patch 8** — Gemini retry resilience in `geminiNativeProvider.ts` (6 retries with exponential backoff up to 60s on 429/502/503/504 + transient network errors, plus a 120s connect timeout). Depends on Patch 7 having run first — uses its sentinel as an anchor.
9. **Patch 9** — Reinstall BYOK-only files that don't exist upstream (canonical copies in `.github/byok-patches/files/` → `src/`).
10. **Patch 10** — Anthropic provider `createClient()` hook + cross-provider failover wrapper in `anthropicProvider.ts`.
11. **Patch 11** — Register `VertexAnthropicLMProvider` and wire it as the Anthropic failover target in `byokContribution.ts`.
12. **Patch 12** — Add VertexAnthropic + Anthropic-fallback settings to `configurationService.ts`.
13. **Patch 13** — Promote `google-auth-library` to a direct dependency (required by `VertexAnthropicLMProvider`).
14. **Patch 14** — Declare `vertexanthropic` (lowercase) as a `languageModelChatProviders` vendor in `package.json`, normalising any stray camelCase variants.
15. **Patch 15** — Short-circuit `switchToBaseModel()` in `chatParticipants.ts` before it looks up `copilot-base`, which throws in BYOK-only mode.
16. **Patch 16** — Tolerate `copilot-fast` unavailability in `VirtualToolGrouper._generateBulkGroupDescriptions` so MCP-heavy workspaces (>128 tools) don't crash the chat turn when `gpt-4o-mini` can't be resolved under the fake-token bypass.
17. **Patch 17** — `sanitizeAnthropicToolId` in `anthropicMessageConverter.ts`. Rewrites cross-provider tool-call ids (e.g. Gemini's `func_call.abc.123`) to match Anthropic's `^[a-zA-Z0-9_-]+$` validator so switching mid-conversation from Gemini to Claude doesn't 400.
18. **Patch 18** — `renderPromptElementJSON` copilot-base fallback in `promptRenderer.ts`. The common trunk every file/edit tool uses to render tool results (read_file, list_dir, file_search, grep_search, get_errors, edit tools via codeMapper, etc.) hard-codes `getChatEndpoint('copilot-base')`, which throws under the fake-token bypass. Wraps the lookup in try/catch and falls back to `getAllChatEndpoints()[0]`.
19. **Patch 19** — VertexAnthropic context defaults + known Claude capability table in `vertexAnthropicProvider.ts`. Bumps default `maxInputTokens` from 100K → 200K (1M for Opus 4.7 / Opus 4.6 / Sonnet 4.6 per Vertex AI's native large-context offering — see [docs.anthropic.com/en/api/claude-on-vertex-ai](https://docs.anthropic.com/en/api/claude-on-vertex-ai)), flips default `vision` from `false` → per-model lookup (true for every Claude family except Claude 3.5 Haiku), and adds an optional `vision?: boolean` override field to `VertexAnthropicModelConfig`. Fixes three symptoms: (a) "prompt silently rejected at 100K"; (b) "vision is not supported by the current model"; (c) context-window circle indicator in the chat UI filling to 100% at ~10% actual utilisation because `maxInputTokens` was understated (auto-compaction fires on time but the ring says "full"). Sonnet 4.5 / Sonnet 4 stay at 200K default — their Vertex 1M window requires the `context-1m-2025-08-07` beta header which changes billing so we don't opt users in silently.
20. **Patch 20** — Self-calibrating chars-per-token ratio in `anthropicProvider.ts`. Replaces the fixed `length/4` estimator in `provideTokenCount` with a per-model EMA ratio that seeds at 3.5 and recalibrates from real `usage.input_tokens` returned by every streamed response. Converges to ground truth within 2-3 turns. Makes the VS Code context-window indicator and tiered auto-compaction thresholds (Patches 4, 6) track Claude's actual tokenizer instead of a generic approximation, which is what users see when they say "the context window doesn't work on BYOK providers".
21. **Patch 21** — Anthropic known-models capability fallback in `anthropicProvider.ts`. Under the BYOK fake-token bypass `_knownModels` (fetched from GitHub and filtered by Copilot subscription entitlement) is almost always empty, so every Anthropic model fell through to a generic fallback hard-coded with `maxInputTokens: 100000`, `vision: false`, `thinking: false`. Introduces a per-family capability table identical in spirit to Patch 19's, so direct-Anthropic Claude models get the same sensible defaults (200K context, vision-on for all except 3.5 Haiku, thinking-on for Opus 4.x / Sonnet 4.x / 3.7 Sonnet).
22. **Patch 22** — Per-request `[BYOK TokenBudget]` info log in `anthropicProvider.ts`. Patch 20's calibration logs at `trace` level, which is invisible under the default `info` log level, so users had no way to verify context-window tracking was working without enabling trace. Emits one info-level line per completed request with real `prompt_tokens`, estimator delta, current chars-per-token ratio, `max_input`, `pct_used`, and applied context edits. Tagged with `providerName` (`Anthropic` vs `VertexAnthropic`) via `(this.constructor as typeof AnthropicLMProvider).providerName` so the same log statement serves both code paths. Grep `[BYOK TokenBudget]` in `~/Library/Application Support/Code/logs/*/exthost*/GitHub.copilot-chat/GitHub Copilot Chat.log` to audit every turn.
23. **Patch 23** — Adaptive compaction thresholds for large-context models in `backgroundSummarizer.ts` + `agentIntent.ts`. After Patch 19 bumped Claude Opus 4.6 / 4.7 / Sonnet 4.6 to their native 1M Vertex context window, Patches 4/6's percentage thresholds (0.70 / 0.80 / 0.90) started firing tier-1 compaction at ~700K real tokens instead of ~140K — per-call cost scales linearly with prompt size at flat Vertex pricing, so this was a ~5x cost regression for the exact same workflow ("keep chatting until auto-compact saves us"). Adds a `resolveCompactionThresholds(modelMaxPromptTokens)` helper that switches models with `modelMaxPromptTokens > 300_000` to absolute-token targets (tier1=180K, tier2=200K, tier3=220K), and threads `this.endpoint.modelMaxPromptTokens` through the `getCompactionTier` call site. 200K-and-smaller models retain the original 0.70/0.80/0.90 ladder. The full 1M window is still reachable — the patch only changes when compaction fires, not whether the window is available as a safety net.
24. **Patch 24** — Always cache system prompt + tools for Anthropic/Vertex BYOK in `anthropicProvider.ts`, with strict 4-breakpoint budget enforcement that **recursively walks nested content**. Upstream's `addCacheBreakpoints` (`cacheBreakpoints.ts`) distributes the 4-breakpoint Anthropic cache budget bottom-up (tool results → current user → leftover on system), so in multi-tool-call turns the system prompt — the largest stable prefix in the conversation, commonly 10-15K tokens with workspace rules + agent template on Opus — never gets marked and re-bills at full 1x rate every turn. The `tools` array is similarly stable across the agent loop but upstream never caches it at all. Marks both at the BYOK layer, **then counts total breakpoints across system + tools + every message content block (including nested text blocks inside `tool_result.content[]`) and strips the oldest message breakpoints until total ≤ 4**. This patch has been broken twice: v1 added our two unconditionally and failed with `Found 6. (invalid_request_error)`; v2 counted only top-level block `cache_control`s and missed nested ones inside `tool_result.content[]` — `anthropicMessageConverter.ts` line ~107 emits `{ type: 'text', text: ' ', cache_control: ... }` as a nested child of a tool_result, and a flat walker would miss it, producing `Found 5` when MCP tools pushed breakpoints into nested content (more tools = more tool_results = more nested breakpoints). v3 (current) recurses through `content?` arrays. Priority order: system (largest stable prefix) > lastTool (stable, never cached by upstream) > recent message breakpoints (tool results / current user). Oldest-first eviction is safe because later cache breakpoints subsume everything before them anyway. Sentinels used to detect an already-patched file: `locateMessageBreakpoints` AND `const walk = (blocks: unknown)` — both must be present. The patch script also contains an upgrade path: any prior Patch 24 block (v1 or v2) is stripped via regex before the recursive-walker version is re-installed, so users running the nightly sync from any broken state get the fix cleanly. Anthropic prompt caching: writes 1.25x, reads 0.1x → break-even after 2 turns, multi-turn agentic loops save ~70% on the shared prefix. Ref: [docs.anthropic.com/en/docs/build-with-claude/prompt-caching](https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching).
25. **Patch 25** — Anthropic retry + readable-error resilience in `anthropicProvider.ts`. Without this, `overloaded_error` (HTTP 529 — Anthropic's "capacity pressure" signal that tends to fire in bursts on Vertex Claude Opus), rate limits (429), transient 5xx and network blips all bubble up as raw JSON blobs into the chat UI (`Reason: {"type":"error","error":{"type":"overloaded_error","message":"Overloaded"},...}: Error: {...}`). The direct Anthropic provider has a Vertex failover path for these classifications but VertexAnthropic primaries have nothing to fail over to. Adds two top-level helpers — `extractReadableAnthropicMessage(err)` (parses the nested `error.message` + `error.type` out of the SDK envelope, or falls back to the first line) and `classifyRetryableAnthropicError(err)` (SDK class + 529/429/502/503/504/5xx + node transport codes + payload sniffing for non-SDK errors) — and wraps `_makeRequest`'s stream-create + consume in a try/catch that retries up to 4 times with 5s → 10s → 20s → 40s backoff (cap 60s, 75s cumulative worst case), reports a `LanguageModelThinkingPart` progress message on each retry so the user sees *why* the chat is waiting, and re-throws a clean readable error on final failure. Only retries when `ttft === undefined` so partial responses never get duplicated. Budget is intentionally tighter than Gemini's 6-retry Patch 8 — direct Anthropic has Vertex as a failover, so burning a full Gemini-style budget before failing over is worse UX than a quick retry-then-fallover cycle.
26. **Patch 26** — Gemini provider `createClient()` hook + subclassable `providerName` in `geminiNativeProvider.ts`. Upstream instantiates `new GoogleGenAI({ apiKey })` inline inside the 300+ line `provideLanguageModelChatResponse`/`doRequest` closure, which means any subclass wanting to route through a different endpoint (Vertex AI under `publishers/google/models/…` instead of `generativelanguage.googleapis.com`) would have to duplicate the entire streaming + OTel + retry pipeline. Widens `providerName` to `string` so subclasses can override the literal, bumps `_requestLogger` / `_telemetryService` / `_otelService` constructor params to `protected`, and extracts client construction into a `createClient(apiKey, model)` protected hook that subclasses override. Mirrors Patch 10's Anthropic surgery. Enables Patch 27 (`VertexGeminiLMProvider`).
27. **Patch 27** — Register `VertexGeminiLMProvider` in `byokContribution.ts`. Mirrors Patch 11's VertexAnthropic wire-up: adds the import next to `vertexAnthropicProvider`, then registers the provider under the `vertexgemini` vendor right after `GeminiNativeBYOKLMProvider` so the two Gemini surfaces sit together. Shares the `knownModels[GeminiNativeBYOKLMProvider.providerName]` table with the native provider since model capabilities (context window, vision) are identical between the two APIs for the same model family.
28. **Patch 28** — Declare `vertexgemini` (lowercase) as a `languageModelChatProviders` vendor in `package.json`, normalising any stray camelCase variants. Parallel to Patch 14. Without this VS Code refuses the `lm.registerLanguageModelChatProvider` call at runtime with "Chat model provider uses UNKNOWN vendor vertexgemini" and the provider never appears in the model picker.
29. **Patch 29** — Add `chat.vertexGeminiModels` setting to `configurationService.ts`. Parallels Patch 12's `VertexAnthropicModels`. The new `VertexGeminiLMProvider` reads from this setting (or per-group `configuration.models` inside `chatLanguageModels.json`) to know which Vertex Gemini model ids to offer, what GCP project/location to route them through, and optional `maxInputTokens` / `maxOutputTokens` / `vision` overrides.
30. **Patch 30** — Install BYOK-only routing classifier files (`.github/byok-patches/files/byokRouting*`). Three-tier cascade (Gemini Flash → Vertex Claude Haiku → regex heuristic) used by the auto-router to cheaply pre-classify chat requests before picking a response-generation model. Types + heuristic live under `src/extension/byok/common/`; the classifier itself under `src/extension/byok/vscode-node/`. Installed via `install_byok_file` so sync doesn't clobber them.
31. **Patch 31** — Empty-stop completion detection in `chatMLFetcher.ts` + clearer message in `commonTypes.ts`. Some models (notably `qwen/qwen3.5-122b-a10b` on GitHub CAPI, and MoE models under load in general) occasionally return a streaming completion with `finishReason=Stop` but zero text content AND zero tool calls. Upstream's `processSuccessfulResponse` treats that as a successful response (Stop is in the `successFinishReasons` set at line ~1761), so the tool-calling loop falls through with nothing to render. Users see a generic "Sorry, no response was returned" / "The model unexpectedly did not return a response" with no actionable recovery and no retry — clicking "Try Again" just reproduces the empty response because neither the chatMLFetcher nor the agent loop knows anything went wrong. This patch (a) adds a `RESPONSE_EMPTY_STOP` reason constant, (b) widens the `Unknown`-case error-details branch in `getErrorDetailsFromChatFetchError` to surface a specific message ("model returned an empty response… try again, or switch to a different model"), and (c) filters empty-stop completions out of the success set in `processSuccessfulResponse` and returns `Unknown`/`RESPONSE_EMPTY_STOP` instead. Because the result is no longer `Success`, the existing `toolCallingLoop.shouldAutoRetry` logic auto-retries once in `autoApprove`/`autopilot` modes, which typically unsticks the model on the first retry. Sentinels: `BYOK CUSTOM PATCH: empty-stop detection` (commonTypes constant), `BYOK CUSTOM PATCH: empty-stop message` (commonTypes error-details branch), `BYOK CUSTOM PATCH: reject empty-stop completions` (chatMLFetcher filter), `BYOK CUSTOM PATCH: empty-stop reason tag` (chatMLFetcher fallthrough return).
32. **Patch 32** — Guard ChatResponseStream proposed-API `.bind` calls in `codeBlockProcessor.ts`. `CodeBlockTrackingChatResponseStream` forwards every stream method by doing `this._wrapped.X.bind(this._wrapped)` unconditionally at class-field init time. When VS Code Stable strips a proposed-API method from the runtime stream for a given host version (observed: `info` is `undefined` on 1.117.0 Stable even though `vscode.d.ts` still declares it), the raw `.bind` throws `TypeError: Cannot read properties of undefined (reading 'bind')` inside the wrapper's constructor, taking down every tool-calling turn before a tool can run (stack top: `new CodeBlockTrackingChatResponseStream → createInstance → runOne → _runLoop → runWithToolCalling → getResult`). Upstream already guards `workspaceEdit` with `?.bind(...) || (() => { })` for exactly this reason; this patch extends the same pattern to `confirmation`, `warning`, `info`, `hookProgress`, `reference2`, `codeCitation`, `anchor`, `externalEdit`, `beginToolInvocation`, `updateToolInvocation`, and `usage`. No-op fallbacks are safe because each method is a pure emitter — skipping a call just means that UI feature is silently unavailable on VS Code versions where the proposal wasn't wired through. Sentinel: `BYOK CUSTOM PATCH: guard every proposed-API member`. Must use function-form `.replace(anchor, () => replacement)` because the replacement payload contains `$` in `|| (() => { })` — see Patch 17's comment for why string-form replacement silently corrupts the file.
34. **Patch 34** — BYOK Auto language-model provider. Upstream's `copilot/auto` is resolved by `AutomodeService`, which POSTs to the CAPI `auto_mode` endpoint with the Copilot session token to pick a concrete model. That path collapses under the BYOK fake-token substitution from Patch 1: the CAPI request 401s, `resolveAutoModeEndpoint` throws, and the UI surfaces "Language model unavailable" on every turn the user selects Auto — removing the one model-selection affordance people reach for most. There's no upstream hook to swap the resolver (the picker hardcodes `vendor === 'copilot'` for the auto entry). Fix: register a parallel `byokauto` vendor with a single `auto` model via a new `BYOKAutoLMProvider` class (canonical copy under `.github/byok-patches/files/byokAutoProvider.ts`, installed into `src/extension/byok/vscode-node/` by `install_byok_file`). On each request the provider reads `chat.byok.auto.defaultModel` (formatted as `vendor/modelId`, e.g. `vertexgemini/gemini-3.1-pro-preview`), resolves the target via `vscode.lm.selectChatModels({ vendor, id })`, and forwards the request through `model.sendRequest(messages, options, token)` — which re-enters the VS Code LM API and dispatches to the actual BYOK provider's `provideLanguageModelChatResponse`. Response stream parts are reported unchanged through the `progress` reporter, preserving tool-call ids, thinking blocks, and token-usage `DataPart`s (Patch 33) intact. `provideTokenCount` defers to the target's tokenizer so agent budget math stays accurate. This is intentionally the "simple" stage (plan B2): one configured fallback, no classifier. The `_resolveTargetModel` method is a protected seam so the follow-up that wires in `ByokRoutingClassifier` (Patch 30) to pick per-request can swap just that method without touching registration or request plumbing. Self-routing is guarded (`byokauto/auto → byokauto/auto` would infinite-loop). Registration patches `byokContribution.ts` alongside the other vendor providers.

35. **Patch 35** — `chat.byok.auto.defaultModel` setting in `configurationService.ts`. String setting consumed by `BYOKAutoLMProvider` (Patch 34) to pick the delegation target. Empty string (default) falls back to the provider's compiled-in default (`vertexgemini/gemini-3.1-pro-preview`) so a fresh install still resolves something provided the Vertex Gemini provider (Patches 26–29) is configured. Sits next to the Anthropic fallback settings declared by Patch 12.

37. **Patch 37** — Declare `byokauto` vendor in `package.json` under `contributes.languageModelChatProviders`. Without this manifest entry the VS Code runtime refuses the `lm.registerLanguageModelChatProvider('byokauto', ...)` call from Patch 34 with "Chat model provider uses UNKNOWN vendor byokauto" — the provider is silently dropped, the picker never shows "BYOK Auto", and selecting the entry (if it leaks in via some other code path) surfaces as "model not available". Classic manifest-vs-runtime mismatch: the code exists, the registration attempt runs, but the VS Code LM host gates every registration on a manifest contribution, same way `anthropic` / `vertexanthropic` / `vertexgemini` / all other BYOK vendors are gated. No `configuration` block because `byokauto` owns no credentials — it delegates to whichever real BYOK provider `chat.byok.auto.defaultModel` points at, and that provider's own entry handles the API-key UI. Mirrors Patch 14 (vertexanthropic) and Patch 28 (vertexgemini) exactly — same Node heredoc pattern, same case-normalisation defensive block, same idempotency check via `providers.some(p => p.vendor === 'byokauto')`. Appended to the end of the provider list rather than slotted next to a specific peer because it's not a concrete model family, it's the meta-router.

36. **Patch 36** — Defensive auto-endpoint resolution in `languageModelAccess.ts`. Two edit sites, same root cause as Patch 34: `AutomodeService.resolveAutoModeEndpoint` dies under BYOK. (a) `_provideLanguageModelChatInfo` unconditionally pushes the resolved auto endpoint into `chatEndpoints`; if the resolver throws (e.g. empty `allEndpoints` because Patch 5 neutralises the CAPI model fetch, or 401 because the fake token is rejected), the throw propagates out and nukes the entire copilot-vendor model listing — the picker shows only whatever the BYOK providers have cached. Fix: wrap the resolver call in `try/catch`, skip the push on failure, log a warning. Also widens `defaultChatEndpoint` to `IChatEndpoint | undefined` so the type doesn't lie when `autoEndpoint` is absent — downstream code only reads `defaultChatEndpoint` inside the `chatEndpoints` for-loop, which runs zero iterations when the list is empty, so no dereference happens. (b) `_getEndpointForModel` hits the same resolver when a stale picker UI still has `copilot/auto` selected; replace the raw `await ...resolveAutoModeEndpoint(...)` with a BYOK-aware error path: empty endpoints yield "pick BYOK Auto or a concrete model", otherwise wrap the resolver call and rethrow with an actionable message. Users now get a recoverable error instead of "Language model unavailable".

33. **Patch 33** — Tunnel BYOK token usage to the context-window ring indicator. Spans four files: adds `TokenUsage = 'token_usage'` mime type in `endpointTypes.ts`; emits `result.usage` as a `LanguageModelDataPart(TokenUsage)` after each successful request in `anthropicProvider.ts` (covers VertexAnthropic via inheritance) and `geminiNativeProvider.ts` (covers VertexGemini via inheritance, plus adds the `LanguageModelDataPart` + `CustomDataPartMimeTypes` imports upstream doesn't already bring in); captures the DataPart mid-stream in `extChatEndpoint.ts` and returns it as the real `usage` record instead of the hardcoded `{prompt_tokens: 0, ...}` upstream falls back to. Without this the context-window ring in the chat UI stays empty across every BYOK turn (the visual cue users rely on to know when auto-compaction is about to fire always reads 0%). This exact change shipped as commit `c240d53a8` on Apr 14 but was never codified as a patch, so the Apr 17 nightly sync reverted it — classic "made a custom edit without writing a patch, got wiped" story. All four edits are idempotent (sentinels `BYOK CUSTOM PATCH: token-usage data part` / `BYOK CUSTOM PATCH: context-window usage tunnel` / `BYOK CUSTOM PATCH: emit TokenUsage to context-window ring`) and gracefully skip with a warning if any anchor goes missing. **Bash heredoc gotcha re-learned here** (affects Patch 32 too, which had to be fixed alongside): when writing `node << 'PATCH_EOF' ... PATCH_EOF` blocks via editing tools that escape whitespace, leading indentation that comes through as literal `\t` (backslash-t, 2 bytes) instead of a real tab (0x09) will parse-fail in Node.js with `SyntaxError: Expected unicode escape` — outside a string literal `\t` is invalid. Fix: edit with real tabs directly, or post-process the script to convert leading `\\t` runs inside heredocs to real tabs. Inside JS string literals `\t` is fine, it's only leading-whitespace indentation that breaks.

30. **Patch 30** — BYOK routing classifier (3-tier cascade) in new files under `byok/common/` and `byok/vscode-node/`. Foundation for a BYOK-native auto-router that routes each incoming chat request to the cheapest *capable* model instead of blindly sticking to Opus for trivial asks. The classifier answers five questions per prompt — `complexity` (trivial/simple/moderate/complex), `task_type` (code_gen/debug/refactor/plan/shell/sql/explain/test/chat), `topic_changed`, `needs_vision`, and `confidence` — so the future router can pick Haiku/Flash for trivial/simple work, Sonnet/Gemini-Pro for moderate, and reserve Opus for genuine `plan`/`complex`. Uses a three-tier cascade so no single provider outage silently forces Opus-for-everything: **Tier 1** = user's Google Gemini API key (cheapest, sub-second, preferred); **Tier 2** = Vertex Claude Haiku 3.5 when Tier 1 rate-limits, times out, or errors (independent failure domain — Gemini native API and Vertex share no front-door); **Tier 3** = pure-regex heuristic (`byokRoutingHeuristics.ts`) that always returns a lower-confidence decision so the router can still make progress offline. Tier timeouts are 800ms (primary) / 1500ms (failover) — deliberately tight because the classifier sits on the hot path for every turn; failures fall through to the heuristic rather than stalling the response. Both LLM tiers use `temperature: 0`, 256-token caps, and structured-output mode (Gemini `responseSchema`, Anthropic system-prompt-enforced JSON) so responses are cheap and machine-parseable. A robust JSON extractor (`parseAndValidate`) strips markdown fences, repairs missing fields from the heuristic, coerces out-of-vocab enum values, and clamps `confidence` to `[0,1]` — the classifier never throws on malformed LLM output. Vertex Haiku routing reuses the same Vertex baseURL / OAuth bearer / `streamRawPredict`→`rawPredict` dance as VertexAnthropic (Patch 10/11's infrastructure). Tests use `ClassifierTestOverrides` to stub out the two network calls deterministically — 39 unit tests cover the heuristic vocabulary, JSON-extraction edge cases, and every cascade transition (Tier 1 success, Tier 1 reject → Tier 2, both fail → heuristic, Tier 1 timeout → Tier 2, no keys configured → heuristic only). Pure-types file (`byokRoutingClassifier.types.ts`) and heuristic live in `byok/common/` with zero SDK deps so they can be imported from anywhere; the actual classifier (which pulls `@google/genai`, `@anthropic-ai/sdk`, `google-auth-library`) lives in `byok/vscode-node/`. Canonical copies under `.github/byok-patches/files/` + `install_byok_file` block — no upstream tree touched.

### BYOK routing classifier files

The classifier introduced by Patch 30 lives entirely in BYOK-only files not
present in upstream; the sync workflow's `rsync --delete` would wipe them each
night without the canonical-copy indirection:

- Canonical copies in `.github/byok-patches/files/`:
  - `byokRoutingClassifier.types.ts` — pure types / interfaces
  - `byokRoutingHeuristics.ts` — regex-based Tier-3 classifier
  - `byokRoutingHeuristics.spec.ts` — heuristic unit tests
  - `byokRoutingClassifier.ts` — 3-tier cascade with SDK calls
  - `byokRoutingClassifier.spec.ts` — cascade unit tests (stubbed network)
- Installed at:
  - `src/extension/byok/common/byokRoutingClassifier.types.ts`
  - `src/extension/byok/common/byokRoutingHeuristics.ts`
  - `src/extension/byok/common/test/byokRoutingHeuristics.spec.ts`
  - `src/extension/byok/vscode-node/byokRoutingClassifier.ts`
  - `src/extension/byok/vscode-node/test/byokRoutingClassifier.spec.ts`

Edit the canonical copies then re-run `apply-byok-patches.sh`. `install_byok_file`
short-circuits identical copies via `cmp -s` so repeated local runs are free.
Run tests with `npx vitest run src/extension/byok/common/test/byokRoutingHeuristics.spec.ts src/extension/byok/vscode-node/test/byokRoutingClassifier.spec.ts`.

### BYOK Auto provider file

The BYOK-native Auto provider from Patch 34 lives entirely in a BYOK-only file
not present upstream:

- Canonical copies:
  - `.github/byok-patches/files/byokAutoProvider.ts` — the provider class
  - `.github/byok-patches/files/byokAutoProvider.spec.ts` — unit tests
- Installed at:
  - `src/extension/byok/vscode-node/byokAutoProvider.ts`
  - `src/extension/byok/vscode-node/test/byokAutoProvider.spec.ts`
  (both via `install_byok_file` in Patch 34's section)

The spec file **must** go through `install_byok_file`: the nightly sync's
`rsync --delete` wiped the un-canonicalised version once already (commit
`ec57dd30f`), erasing 301 test lines overnight.

Edit the canonical copy and re-run `apply-byok-patches.sh`. The provider
exposes a single protected seam, `_resolveTargetModel(token)`, designed for
the future classifier-driven router (B3) to override — keep that signature
stable if you extend it. The paired upstream edits live in Patch 34
(register in `byokContribution.ts`), Patch 35 (settings declaration in
`configurationService.ts`), and Patch 36 (defensive guards in
`languageModelAccess.ts`).

### Vertex Gemini files

Like VertexAnthropic, the new Gemini-via-Vertex provider lives in a BYOK-only
file that the sync workflow doesn't touch directly:

- Canonical copy: `.github/byok-patches/files/vertexGeminiProvider.ts`
- Installed at: `src/extension/byok/vscode-node/vertexGeminiProvider.ts`
  (via the `install_byok_file` block in Patch 9's section)

If you need to edit the VertexGemini provider, edit the canonical copy and
re-run `apply-byok-patches.sh`, or edit both in lockstep during development.
The `cmp -s` guard in `install_byok_file` short-circuits identical copies so
local iteration isn't hurt by repeated script runs.

When you add a new patch, bump the number and add an entry here.

## Known Gotchas (recurring upstream regressions)

These are upstream behaviors that keep silently re-breaking the fork across
nightly syncs. Each one is covered by a patch above — if the patch stops
working, audit this list before writing a new patch.

### `engines.vscode` bump

Upstream tracks VS Code Insiders and regularly bumps `package.json` →
`engines.vscode` (e.g. `^1.118.0`, `^1.119.0`). If this ships to the fork
without being clamped, installing the `.vsix` on VS Code Stable fails with:

> Error: Unable to install extension 'github.copilot-chat' as it is not
> compatible with VS Code '1.116.0'.

**Fix:** Covered by **Patch 3**, which forces `engines.vscode = "^1.116.0"`
on every sync. If you intentionally want to bump the floor (e.g. because you
want to use a new `vscode` API), update Patch 3 — never edit `package.json`
directly, the nightly sync will undo it.

### Gemini SDK error ergonomics

The `@google/genai` SDK throws `ApiError` whose `.message` is the raw HTTP
JSON body. It also has no built-in retries for 429/503. Without Patches 7+8
applied, chat UI shows `Reason: {"error":{"code":503,...}}` and fails
immediately on transient 503s.

**Fix:** Patches 7 and 8 in `geminiNativeProvider.ts`. Patch 8 depends on
Patch 7 — if you rewrite Patch 7, re-check Patch 8's anchors.

### Fake token 401 spam

When running in fully offline BYOK mode the fake Copilot token 401s against
`api.githubcopilot.com`, causing log spam and retry hot-loops in
`modelMetadataFetcher.ts` and friends.

**Fix:** Patches 1 and 5 add short-circuits for `copilotToken === 'fake-token'`.

**Subtle perf trap** (fixed in Patch 5): the short-circuit must NOT fire
`_onDidModelRefresh`. In BYOK-only mode `_familyMap` stays empty, so
`_shouldRefreshModels()` returns `true` on every call. If the bypass fires
the refresh event, `languageModelAccess`'s listener re-queries models →
re-enters `_fetchModels` → fires the event again. Cheap per-iteration but
cumulatively slows VS Code. Patch 5 also sets a `_fakeTokenShortCircuited`
flag so subsequent calls return immediately without even awaiting
`getCopilotToken()`.

### Cross-provider tool-call ID incompatibility

Anthropic validates `tool_use.id` and `tool_result.tool_use_id` against
`^[a-zA-Z0-9_-]+$`. Gemini emits ids containing `.`, `/`, etc. When a user
switches mid-conversation from Gemini to Claude (or Vertex-Claude), the
historical tool-call ids flow through unchanged and Anthropic 400s with:

> messages.N.content.M.tool_use.id: String should match pattern
> '^[a-zA-Z0-9_-]+$'

**Fix:** Patch 17 exports a deterministic `sanitizeAnthropicToolId` helper
used at both the `tool_use` and `tool_result` emission sites in
`anthropicMessageConverter.ts`. Valid ids pass through unchanged; invalid
ones get `_`-replaced plus an FNV-1a hash suffix so distinct original ids
don't collapse to the same sanitized string.

### Internal model family lookups throwing in BYOK-only mode

Several code paths hard-code an internal Copilot model family and call
`getChatEndpoint('copilot-base')` / `getChatEndpoint('copilot-fast')` (the
latter aliases to `gpt-4o-mini`). Under the fake-token bypass these lookups
throw because `modelMetadataFetcher._familyMap` / `_copilotBaseModel` are
never populated. Symptoms users see:

> Error: Unable to resolve chat model with family selection: copilot-base  
> Error: Unable to resolve chat model with family selection: gpt-4o-mini

The second one is particularly sneaky because it only surfaces when the
workspace registers ≳128 tools (e.g. Pylance + Looker MCP + Tableau MCP all
loaded), tipping `VirtualToolGrouper` into its LLM-summarization path.

**Fix:** Patch 15 reorders `switchToBaseModel` to short-circuit BYOK / free
/ non-copilot requests before the `copilot-base` lookup. Patch 16 wraps the
`copilot-fast` lookup in `_generateBulkGroupDescriptions` in try/catch and
falls back to deterministic group descriptions so chat turns still complete.
Patch 18 covers the third (and most impactful) site: `renderPromptElementJSON`
in `promptRenderer.ts`, which every built-in file tool routes through. Without
it the chat looks superficially healthy (turns complete, model answers) but
`read_file` / `list_dir` / `file_search` / `grep_search` / `get_errors` /
edits via `codeMapper` all fail with "Unable to resolve chat model with family
selection: copilot-base", typically manifesting as the assistant blaming MCP,
workspace indexing, or your Copilot subscription for a problem that is
entirely in the BYOK fake-token path.
